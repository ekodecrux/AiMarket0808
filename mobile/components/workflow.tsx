import { ReactNode } from "react";
import { FlatList, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { c } from "@/components/nexus";
export function Field({ label, value, onChangeText, placeholder, multiline, numeric }: { label: string; value: string; onChangeText: (value: string) => void; placeholder: string; multiline?: boolean; numeric?: boolean }) { return <View style={s.field}><Text style={s.label}>{label}</Text><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor="#94A3B8" multiline={multiline} keyboardType={numeric ? "decimal-pad" : "default"} textAlignVertical={multiline ? "top" : "center"} style={[s.input, multiline && s.multiline]} /></View>; }
export function Segments<T extends string>({ value, items, onChange }: { value: T; items: { value: T; label: string }[]; onChange: (next: T) => void }) { return <View style={s.segmentShell}><FlatList horizontal data={items} keyExtractor={(item) => item.value} showsHorizontalScrollIndicator={false} contentContainerStyle={s.segmentList} renderItem={({ item }) => <Pressable onPress={() => onChange(item.value)} style={({ pressed }) => [s.segment, value === item.value && s.segmentActive, pressed && { opacity: 0.7 }]}><Text style={[s.segmentText, value === item.value && s.segmentTextActive]}>{item.label}</Text></Pressable>} /></View>; }
export function Row({ children }: { children: ReactNode }) { return <View style={s.row}>{children}</View>; }
export const s = StyleSheet.create({ field: { gap: 5 }, label: { color: c.ink, fontSize: 10, fontWeight: "900", letterSpacing: 0.8 }, input: { minHeight: 47, paddingHorizontal: 12, borderRadius: 12, borderWidth: 1, borderColor: c.border, backgroundColor: c.canvas, color: c.ink, fontSize: 14 }, multiline: { minHeight: 78, paddingTop: 10 }, segmentShell: { backgroundColor: "#EAF0F8", padding: 4, borderRadius: 14 }, segmentList: { gap: 4 }, segment: { minHeight: 37, paddingHorizontal: 13, borderRadius: 10, justifyContent: "center" }, segmentActive: { backgroundColor: c.white }, segmentText: { color: c.muted, fontSize: 12, fontWeight: "900" }, segmentTextActive: { color: c.blue }, row: { flexDirection: "row", justifyContent: "space-between", alignItems: "flex-start", gap: 10 } });
